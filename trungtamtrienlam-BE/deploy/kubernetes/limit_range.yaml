apiVersion: v1
kind: LimitRange
metadata:
  name: default-limit
  namespace: kube-system
spec:
  limits:
  - default:
      memory: 256Mi
      cpu: 100m
    type: Container

# -----

apiVersion: v1
kind: LimitRange
metadata:
  name: default-limit
  namespace: default
spec:
  limits:
  - default:
      memory: 256Mi
      cpu: 100m
    type: Container
