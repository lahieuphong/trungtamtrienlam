[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py tests.py *_tests.py
testpaths = tests
addopts = -ra --strict-config --strict-markers -p no:cacheprovider
markers =
    smoke: fast checks that do not require PostgreSQL, Redis, or other services
